suite: buildbarn labels and availability defaults
values:
  - ../ci-values/ci.yaml
templates:
  - templates/deployment-frontend.yaml
  - templates/statefulset-storage.yaml
  - templates/deployment-worker-ubuntu22-04.yaml
  - templates/pdb.yaml
tests:
  - it: gives frontend stable labels and preferred pod anti-affinity
    template: templates/deployment-frontend.yaml
    set:
      commonLabels:
        app.kubernetes.io/component: must-not-override-component
      podLabels:
        owner: platform
        app: must-not-override-selector
        helm.sh/chart: must-not-roll-with-chart
        app.kubernetes.io/version: must-not-roll-with-version
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: frontend
      - equal:
          path: spec.selector.matchLabels.app
          value: frontend
      - equal:
          path: spec.template.metadata.labels.app
          value: frontend
      - equal:
          path: spec.template.metadata.labels.owner
          value: platform
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/name"]
          value: buildbarn
      - isNotNull:
          path: spec.template.metadata.labels["app.kubernetes.io/instance"]
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/component"]
          value: frontend
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/part-of"]
          value: buildbarn
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/managed-by"]
          value: Helm
      - notExists:
          path: spec.template.metadata.labels["helm.sh/chart"]
      - notExists:
          path: spec.template.metadata.labels["app.kubernetes.io/version"]
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution[0].weight
          value: 100
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution[0].podAffinityTerm.labelSelector.matchLabels.app
          value: frontend
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution[0].podAffinityTerm.topologyKey
          value: kubernetes.io/hostname
      - notExists:
          path: spec.template.spec.affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution

  - it: keeps storage anti-affinity hard-required
    template: templates/statefulset-storage.yaml
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: storage
      - equal:
          path: spec.selector.matchLabels.app
          value: storage
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/component"]
          value: storage
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution[0].labelSelector.matchLabels.app
          value: storage
      - equal:
          path: spec.template.spec.affinity.podAntiAffinity.requiredDuringSchedulingIgnoredDuringExecution[0].topologyKey
          value: kubernetes.io/hostname
      - notExists:
          path: spec.template.spec.affinity.podAntiAffinity.preferredDuringSchedulingIgnoredDuringExecution

  - it: labels worker pods with their component and pool
    template: templates/deployment-worker-ubuntu22-04.yaml
    set:
      workerUbuntu2204.enabled: true
    asserts:
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: worker
      - equal:
          path: metadata.labels["hermetiq.com/worker-pool"]
          value: ubuntu22-04
      - equal:
          path: spec.template.metadata.labels["app.kubernetes.io/component"]
          value: worker
      - equal:
          path: spec.template.metadata.labels["hermetiq.com/worker-pool"]
          value: ubuntu22-04

  - it: enables storage and frontend disruption budgets by default
    template: templates/pdb.yaml
    asserts:
      - equal:
          path: spec.maxUnavailable
          value: 1
        documentSelector:
          path: metadata.name
          value: storage
      - equal:
          path: spec.selector.matchLabels.app
          value: storage
        documentSelector:
          path: metadata.name
          value: storage
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: storage
        documentSelector:
          path: metadata.name
          value: storage
      - equal:
          path: spec.maxUnavailable
          value: 1
        documentSelector:
          path: metadata.name
          value: frontend
      - equal:
          path: spec.selector.matchLabels.app
          value: frontend
        documentSelector:
          path: metadata.name
          value: frontend
      - equal:
          path: metadata.labels["app.kubernetes.io/component"]
          value: frontend
        documentSelector:
          path: metadata.name
          value: frontend

  - it: allows frontend preferred pod anti-affinity to be disabled
    template: templates/deployment-frontend.yaml
    set:
      frontend.podAntiAffinity.enabled: false
    asserts:
      - notExists:
          path: spec.template.spec.affinity

  - it: allows storage required pod anti-affinity to be disabled
    template: templates/statefulset-storage.yaml
    set:
      storage.podAntiAffinity.enabled: false
    asserts:
      - notExists:
          path: spec.template.spec.affinity
